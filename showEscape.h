#include <iostream>
#include <vector>
#include <random>
#include <mcpp/mcpp.h>

//Function to print escape route in terminal
void printRoute(std::vector<std::vector<char>> escapeRoute, mcpp::Coordinate playerLoc) {
    int steps = escapeRoute.size();

    for (int i = 0; i < steps; i++) {
        std::cout << "STEP[" << i + 1 << "]: " << "(" << 
    }

}

//Function to find escape route
std::vector<std::vector<std::pair<int, int>>> escapeRoute() {

}

//Function to set blocks in MC
void routeInMC(){
    
}